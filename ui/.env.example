# Docker PULSE UI Environment Variables
# Create .env.local with these values in the same directory.

# ===========================================
# API Configuration (Docker Compose)
# ===========================================
# Points to the local FastAPI backend
NEXT_PUBLIC_API_URL=http://localhost:8050

# ===========================================
# Environment Settings
# ===========================================
NEXT_PUBLIC_ENV_NAME=development

# AI Provider display (for UI)
NEXT_PUBLIC_AI_PROVIDER=openai

# ===========================================
# Feature Flags
# ===========================================
# Admin UI gating
NEXT_PUBLIC_ENABLE_ADMIN=true

# Training UI gating
NEXT_PUBLIC_ENABLE_TRAINING=true

# ===========================================
# Development Mode Settings
# ===========================================
# Enable dev mode to use test session ID
NEXT_PUBLIC_USE_DEV_SESSION=false

# Test session ID for development
NEXT_PUBLIC_DEV_SESSION_ID=dev-test-session-001

# ===========================================
# To generate a hex string of 32 characters for JWT secret, run:
# LC_ALL=C tr -dc 'A-Za-z0-9' </dev/urandom | head -c 32; echo
# ===========================================
